# prompttest

A testing framework for LLM prompts that detects regressions when prompts change.

## Install

```bash
pip install -e ".[dev]"
```

## Quick start

```bash
prompttest init
prompttest run
```
